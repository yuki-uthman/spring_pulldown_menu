name: spring_pulldown_menu
description: >-
  A springy, iOS/UIKit-style "..." context menu button for Flutter — a
  frosted-glass popup menu with spring pop-in, directional press-lean,
  release bounce, and native-feel dismiss behavior.
version: 0.2.0
homepage: https://github.com/yuki-uthman/spring_pulldown_menu
repository: https://github.com/yuki-uthman/spring_pulldown_menu
issue_tracker: https://github.com/yuki-uthman/spring_pulldown_menu/issues

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
